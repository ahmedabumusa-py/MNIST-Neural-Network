# MNIST-Neural-Network
A 3 -Layer Neural Network that can be trained on the Fashion MNIST dataset. The program can run predictions on real life images, using your trained model. It can also run your model against the test set to determine its true test accuracy.
